Recovery: `npm ci && npx playwright install chromium && npx drizzle-kit push --force && make typecheck test test-contracts && make smoke-test && make test-stability`. Start: `npm run build && npm start` (supervisor spawns worker+scheduler) or run `npm run worker` / `npm run scheduler` separately. Local template runs need `BROWSERFLOW_NETWORK_PRIVATE_ALLOWLIST=127.0.0.1` and the app on :3000.
