# STATUS — Release 1: PARTIAL (production core implemented and verified; see gaps)
Branch: feat/initial-production-platform. Stack adapted to TypeScript (ADR-0001).
## Delivered & verified
Auth (Argon2id, sessions, CSRF, rate limit, audit, CLI reset) · schema (18 tables, drizzle) · Flow/Draft/immutable FlowVersion with checksums, import/export, duplicate, archive, restore · Compiler (schema, ids, types, config, bindings, handles, entry, cycles, reachability, loop subgraphs, dangerous URLs, unsafe expressions, limits, estimate) · deterministic engine (if/foreach nested scopes/finally/return/error policies/retry/timeouts/cancel) · execution/attempt/node/event/outbox persistence with atomic sequences · DB lease (SKIP LOCKED), heartbeat, worker-lost recovery, anti-stale-worker guards · independent worker & scheduler processes · Playwright Chromium BrowserSession with request/websocket policy, page limits, screenshots, cleanup · identities with DB+file lock and persistent profiles · AES-256-GCM credentials + lazy CredentialRef resolution + redaction · local artifacts with SafePath + quotas · HTTP node with DNS/redirect SSRF checks · scheduler (cron/once/timezone/misfire/overlap/dedupe/outbox publisher/reaper/cleanup) · SSE replay with lastSequence · full UI (login, dashboard, flows, editor, executions, detail with live log/screenshots, schedules, credentials, identities, templates, settings, system) · 9 templates (8 run in E2E, login template verified via live API with credential) · AI boundary (disabled/fake-test-only) · Docker/Compose/Makefile/CI · CLI (admin, doctor, cleanup, backup, restore, smoke) · docs.
## Gates
A Buildable: pass (typecheck, lint, build, migrations, tests). Docker build not executable in sandbox.
B Functional: pass (all items exercised on the live deployment and via scripts).
C Production candidate: partial — worker-lost, cancel, timeout, event ordering, anti-stale, 110-run stability, backup/restore(dry-run) pass; not executed here: Docker/Compose smoke, Redis/Postgres outage chaos, WebSocket long disconnect chaos, 10 long tasks, coverage thresholds, SBOM.
## Next
Run `make docker-build compose-validate` on a Docker host; add coverage tooling; add chaos scripts for DB outage & supervisor restarts; add editor component tests.
