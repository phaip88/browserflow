# BrowserFlow 用户指南（中文）

1. 在顶栏切换 English / 中文。
2. 使用 `browserflow admin create` 创建的管理员登录。
3. 创建 Flow，在画布上拖放节点并连接边。
4. 保存 Draft（支持自动保存）。
5. 编译；存在 ERROR 诊断时不能发布。
6. 发布不可变 FlowVersion。
7. 立即运行或创建定时计划。
8. 在执行详情中查看事件、截图和产物。
9. 凭据加密存储，Flow 只保存 CredentialRef。
10. Identity 使用独立 Profile，同一时刻只能被一个执行锁定。
