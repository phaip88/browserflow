# Verification evidence
`make verify` (or the individual targets) produces `artifacts/verification/*`. All files are generated by real command runs; see `.agent/TEST_RESULTS.md` for the latest recorded run and known gaps (coverage.xml, sbom, docker image build require tooling not present in the authoring sandbox).
